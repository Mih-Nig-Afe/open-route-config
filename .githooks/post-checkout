#!/usr/bin/env sh
# $1=prev HEAD, $2=new HEAD, $3=branch checkout flag (1=branch, 0=file)
# Continue also watches branch changes in IDE — this covers pull/merge when IDE is closed
exec "$(git rev-parse --show-toplevel)/scripts/continue-reindex.sh"
